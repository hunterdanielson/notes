var minimist = require('minimist');

const parse = (flag) => {
    if(flag === 'add')
    {
        console.log('hello');
    }
};

const valid = () => {
    
};

module.exports = {
  parse,
  valid
};
